# Aeoleaf 后台待办

> 每项必须在功能、权限、安全和回归测试全部通过后才能勾选。

## P0：内容安全、可恢复、可追踪

### 1. 自动保存与历史版本

- [x] 为文章和作品建立统一版本表，保存完整快照、来源和时间
- [x] 编辑页本地草稿自动保存，刷新或意外关闭后可恢复
- [x] 已存在内容定时保存服务端草稿，避免产生大量重复版本
- [x] 正式保存或发布前自动生成历史版本
- [x] 历史版本列表支持查看时间、来源和内容摘要
- [x] 支持当前版本与历史版本字段级对比
- [x] 支持恢复历史版本，恢复前再次保存当前快照
- [x] 补充操作者标识，并覆盖并发修改、断网和超大 Markdown 的测试

**验收：** 编辑中断后内容可找回；任意一次正式保存都能查看并安全回滚。

### 2. 回收站

- [x] 文章、作品和留言由硬删除改为软删除
- [x] 默认的前台、API、搜索、RSS、站点地图和后台列表排除已删除内容
- [x] 媒体删除时移动到隔离目录，不立即销毁文件
- [x] 建立统一回收站页面，支持类型、关键词和删除时间筛选
- [x] 支持单项及批量恢复
- [x] 支持二次确认后的彻底删除
- [x] 支持保留期限设置和到期清理，默认不自动开启
- [x] 恢复时处理 slug 冲突、媒体路径冲突和引用关系

**验收：** 普通删除不会丢失数据或文件；恢复后原有页面和资源可以正常访问。

### 3. 备份与恢复

- [x] 备份前执行 SQLite WAL checkpoint，确保数据库快照一致
- [x] 备份数据库、上传目录和必要配置清单
- [x] 生成备份元数据：版本、大小、校验值、创建时间和触发方式
- [x] 后台支持手动创建、查看、下载和删除备份
- [x] 恢复前自动创建安全备份
- [x] 恢复时校验文件格式、校验值和数据库完整性
- [x] 恢复操作需要再次输入管理员密码确认
- [x] 提供定时备份配置、保留数量和失败记录
- [x] 使用临时副本完成恢复演练测试，不直接拿生产数据库试错

**验收：** 能从后台生成完整备份，并在全新临时目录中恢复出可启动、数据一致的站点。

### 4. 统一操作审计

- [x] 建立只追加的通用审计表
- [x] 记录登录成功/失败、退出和会话撤销
- [x] 记录文章、作品、留言、媒体和设置的新增、修改、删除、恢复
- [x] 记录备份创建、下载、删除和恢复
- [x] 保存操作类型、对象、结果、IP、User-Agent、请求 ID 和时间
- [x] 对修改操作保存脱敏后的变更摘要，不记录密码和会话密钥
- [x] 审计页面支持时间、动作、对象和结果筛选及详情抽屉
- [x] 审计记录禁止通过普通后台接口修改或删除

**验收：** 所有 P0 敏感操作都能追溯到具体时间、会话、对象和结果，且不泄露秘密字段。

### 5. 登录安全中心

- [x] 为会话补充创建时间、最后活动时间、IP、设备和 User-Agent 信息
- [x] 安全中心显示当前会话和其他活跃会话
- [x] 支持撤销单个会话及“退出其他设备”
- [x] 在现有 IP 限流基础上增加连续失败记录和短时锁定提示
- [x] 登录成功后轮换 Session ID，退出后立即失效（保留并补测试）
- [x] 敏感操作增加近期登录校验
- [x] 设计并实现通行密钥/WebAuthn；保留环境密码作为恢复入口
- [x] 增加异常登录和安全事件提示

**验收：** 管理员能看见并撤销全部登录会话；暴力尝试被限制；敏感操作需要重新确认身份。

## P0 实施顺序

1. 数据库迁移与统一审计基础
2. 自动保存与历史版本
3. 回收站及现有查询改造
4. 备份、校验与恢复演练
5. 会话元数据、安全中心和通行密钥
6. Shadcn Admin 风格管理页面整合
7. 全量测试、备份恢复演练和部署文档更新

P0 已完成。生产部署后仍需按 `DEPLOYMENT.md` 使用真实 Windows Hello、Touch ID 或安全密钥完成一次人工 WebAuthn 验收。

## P1

- [x] 定时发布与内容预览
- [x] SEO 控制中心
- [x] 媒体库分类、引用分析与图片优化

P1 已完成：预约发布与自动下线按分钟执行；未发布预览链接带签名且 1 小时过期；SEO 支持全站默认值、单篇覆盖、检查面板、noindex、Canonical、Open Graph、结构化数据、Sitemap 与 RSS；媒体库支持用途分类、引用筛选、内容哈希重复检测、尺寸/体积诊断，以及保留原图的 WebP 优化副本。

## P2

- [x] 通知中心
- [x] 系统健康检查
- [x] 高级访客分析

P2 已完成：通知中心汇总发布、备份、安全、留言审核和健康状态事件，支持筛选、已读与未读徽标；系统健康每 5 分钟检查数据库、磁盘、上传目录、备份、Node.js、安全配置与内存，并保留最近 500 次快照、在状态变化时通知；高级访客分析支持周期对比、来源归因、UTM 活动、7 日回访留存、内容访问漏斗、路径转移以及文章深度阅读参与率。

P0、P1、P2 功能开发与本地自动化/浏览器验收均已完成。生产环境仍需按 `DEPLOYMENT.md` 执行部署后人工验收。
